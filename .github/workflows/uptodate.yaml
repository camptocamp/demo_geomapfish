---
name: Is up to date

on:
  push:
    branches:
      - prod-2-7-advance
  pull_request:

env:
  REF_BRANCH: prod-2-7

jobs:
  main:
    runs-on: ubuntu-20.04
    name: Is up to date
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v2
      - uses: actions/checkout@v2
        with:
          ref: ${{ env.REF_BRANCH }}
          path: ref

      - run: mv ref ../
      - run: ci/cleanup .
      - run: ci/cleanup ../ref
      - run: rm ci/cleanup

      - run: diff --recursive --new-file ../ref .
        if: always()
