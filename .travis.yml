---

sudo: required

addons:
  apt:
    packages:
      - python3-netifaces

script:
  - sudo mkdir -p /var/sig
  - sudo echo test >> /var/sig/amazonses_smtp.txt
  - sudo echo test >> /var/sig/amazonses_smtp.txt
  - sudo echo test >> /var/sig/amazonses_smtp.txt
  - sudo echo test >> /var/sig/amazonses_smtp.txt
  - ./docker-run --share=/var/sig make --makefile=demo.mk checks
