#!/bin/bash -e

find /etc /mapcache /project /usr/local/tomcat/webapps/ROOT/print-apps -name '*.tmpl' -print | while read file
do
    echo "Evaluating template '${file}'"
    envsubst < ${file} > ${file%.tmpl}
done

exec "$@"
