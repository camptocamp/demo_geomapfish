---
checks:
  black: False
  isort: False
  codespell: False
  eof: False
  required_workflows: False

publish:
  docker:
    images:
      - group: config
        name: camptocamp/geomapfishdemo-config
